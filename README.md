# vscode-npm-scanner-macos
scans for all node modules bundled in and associated with a visual studio code installation on macos, including asars and extensions

## Usage
- `python3 scan_vscode_node_modules.py > output.csv`
- `open output.csv`
